#ifndef _STRING_H
#define _STRING_H

#include <stddef.h>

size_t strlen(char*);
int strcmp(char*, char*);
int strcpy(char*, char*);
char* strchr(char*, int);
int memcpy(void *, const void*, size_t);


#endif
